export default function BriefingPage() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-lg font-medium text-zinc-900">Briefing</h1>
      <p className="mt-2 text-sm text-zinc-600">
        Every drafted agent action across all areas, ranked by impact and
        consolidated by account – not a raw activity queue. Every action
        stays a draft; nothing is ever sent automatically.
      </p>
      <div className="mt-6 rounded-xl border border-dashed border-zinc-300 bg-zinc-50 p-4 text-sm text-zinc-500">
        Not built yet – depends on the agent core (Stage 5), not started.
      </div>
    </div>
  );
}
