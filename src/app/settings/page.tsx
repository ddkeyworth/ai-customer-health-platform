export default function SettingsPage() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-lg font-medium text-zinc-900">Settings</h1>
      <p className="mt-2 text-sm text-zinc-600">
        Org profile and branding, team and roles, localisation, billing
        (illustrative), integrations (concept-only connectors), developer/API
        (concept-only), consumption and outcome configuration, data export.
      </p>
      <div className="mt-6 rounded-xl border border-dashed border-zinc-300 bg-zinc-50 p-4 text-sm text-zinc-500">
        Not built yet.
      </div>
    </div>
  );
}
